import 'package:flutter/material.dart';

class OpenBusinessAccount extends StatelessWidget {
  const OpenBusinessAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
