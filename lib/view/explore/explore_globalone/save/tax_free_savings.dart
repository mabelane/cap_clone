import 'package:flutter/material.dart';

class TaxFreeSavings extends StatelessWidget {
  const TaxFreeSavings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
