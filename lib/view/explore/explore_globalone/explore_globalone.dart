import 'package:flutter/material.dart';

class ExploreGlobalOne extends StatelessWidget {
  const ExploreGlobalOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
