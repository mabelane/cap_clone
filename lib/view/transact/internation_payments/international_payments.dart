import 'package:flutter/widgets.dart';

class InternationalPayments extends StatelessWidget {
  const InternationalPayments({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
