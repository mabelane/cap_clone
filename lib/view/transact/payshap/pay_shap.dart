import 'package:flutter/widgets.dart';

class PayShap extends StatelessWidget {
  const PayShap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
