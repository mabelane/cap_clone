import 'package:flutter/widgets.dart';

class PlayLotto extends StatelessWidget {
  const PlayLotto({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
