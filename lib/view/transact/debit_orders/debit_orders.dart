import 'package:flutter/widgets.dart';

class DebitOrders extends StatelessWidget {
  const DebitOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
