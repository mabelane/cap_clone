import 'package:flutter/material.dart';

class TempPlaceholder extends StatelessWidget {
  const TempPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
